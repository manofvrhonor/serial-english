# 📘 Карта проекта «Serial English» — для агента

Живой документ. Читать **каждую сессию**. Не раздувать — историю писать в `docs/agent-changelog.md`.

| Файл | Когда читать |
|------|----------------|
| **`agent-plan.md`** (этот) | Каждая сессия: статус, архитектура, gotchas, структура |
| `docs/agent-spec.md` | Смена архитектуры, SRS, импорт, модель данных |
| `docs/agent-changelog.md` | «Когда и зачем сделали», история коммитов |
| `docs/backend-spec.md` | Путь B: бэкенд (модель данных, API, синхронизация) |
| `docs/deploy-runbook.md` | Деплой бэкенда (Timeweb + Dockploy, занятия 6-7) |
| `docs/path-b-runbook.md` | Команды для ручного запуска Пути B (dev-ветка, локальный запуск) |

---

## 0. Быстрый старт

1. Проект: `D:\VIBECODING\SERIAL ENGLISH`
2. Прочитать **этот файл** целиком (~3 мин)
3. Сообщение в чат: цель + `@agent-plan.md` + задача
4. Не сканировать `data/*.json` — только нужные `js/` файлы
5. Dev: `python -m http.server 8081` → http://localhost:8081
6. Прод: https://manofvrhonor.github.io/serial-english/
7. Asset version: **`20260675`** в ветке `dev` (Путь B, закоммичено); на проде (`main`) — `20260673`. Источник: `js/version.js`; bump при правках JS/CSS

**После сессии:** обновить §1 и §7 здесь; 5–15 строк в `docs/agent-changelog.md`.

---

## 1. Статус

| | |
|---|---|
| MVP (этапы 1–8) | ✓ готов |
| UI / дизайн (этап 9) | ✓ готов |
| Публикация для теста | ✓ GitHub Pages |
| GitHub | manofvrhonor/serial-english |
| Lovable-референс | `D:\VIBECODING\word-weaver-offline` |
| Путь B (бэкенд/аккаунты/синхронизация) | ⏳ закоммичено в `dev`, локально протестировано (вход/синхр./библиотека/Alembic); merge в `main` — после теста |

**Текущая фаза:** **Путь B** — добавлен бэкенд (`backend/`, FastAPI) + аккаунты/синхронизация
на фронте (секция в Настройках). Аддитивно: без адреса сервера приложение работает как оффлайн.
См. план `serial_english_path_b`, `docs/backend-spec.md`, `docs/path-b-runbook.md`.

**На проде (main):** `?v=20260673` (Путь B ещё не залит; merge — после теста на `dev`).

**Не переписывать без необходимости:** `js/core/`, `js/db/`, `js/import/`.

---

## 2. Архитектура и решения (ПОЧЕМУ так)

> При правке UI не возвращать «как в Lovable» или «как проще» — сверяться с обоснованием.

### 2.1 Информационная архитектура

| Решение | Почему |
|---------|--------|
| Единый хаб **База знаний** (не отдельные «Слова»/«Фразы» в меню) | Меньше дублирования; стоп-лист и статистика рядом с карточками |
| Стоп-лист и блок «База» в **База знаний**, не в Настройках | Настройки = только системное: интервалы, backup, HARD RESET |
| **Сериалы** и **Книги** — отдельные пункты меню | Фокус продукта — подготовка к серии/главе; отличие от Lovable |
| В UI **«Слово»**, в данных **`lemma`** | Понятнее пользователю; поле в JSON не переименовывать |
| Настройки: только интервалы, экспорт/импорт, HARD RESET | Всё остальное — рабочие разделы, не конфиг |

### 2.2 Shell: desktop и mobile

| Решение | Почему |
|---------|--------|
| Desktop: **sidebar** слева; toggle в **низу** sidebar (`.sidebar-footer`) | Контент на всю высоту; header убран — занимал ~3rem без пользы |
| Нет `desktop-header`, нет «Оффлайн словарь» | Лишняя строка на каждой вкладке |
| Mobile: **6 кнопок** в `mobile-nav` (Каталог·Словарь·Тренировка·Настройки + …) | Прямой доступ; «Каталог» объединяет импорт/сериалы/книги |
| Нет `mobile-header` | Дублировал активный пункт нижнего меню |
| `h1.view-title-section` **скрыт на mobile** | Заголовок = пункт nav; остаётся подзаголовок-описание |
| Скролл в **`.content`**, не `window` | `html,body { overflow:hidden }`, `.app { height:100dvh }` — иначе ломается layout и scroll-top |
| Дизайн: oklch-токены, **card-layout** | Референс Lovable; не откатывать к «голому» HTML |

### 2.3 Списки и таблицы (База знаний)

| Решение | Почему |
|---------|--------|
| **Слова / Выражения** — переключатель **сверху** (не только в «На изучении») | Статистика и списки всегда про один тип; меньше путаницы |
| «К повторению сегодня» = `countTrainingItems(…, dueOnly: true)` по типу | Один элемент, не сумма EN→RU и RU→EN |
| Вкладки **Выучено / Изучать / Стоп / Без перевода** — цвет по статусу; **счётчик под подписью** (2× крупнее) | Верхние теги-статистика убраны; числа в кнопках фильтра |
| «В работе» = все не выученные на вкладке «Изучать» | Включая слова без перевода |
| Фильтр **«Без перевода»** — отдельная вкладка | Счётчик `noTransWords` / `noTransPhrases`; дополнить переводы вручную |
| Поиск в Выучено/Стоп-лист: обновлять только `#kb-content` | Иначе теряется фокус в `#k-search` |
| Действия ✓/стоп на «На изучении»: `refreshKbStats()` в `onChange` панели | Счётчики в шапке без перезахода на вкладку |
| **Один** поиск `#k-search`; кнопка **«Добавить»** только если запрос не пуст и **0 совпадений** в текущей вкладке | Нет дублирующих inline-форм; добавление через модал |
| Добавление: модал `#kb-add-modal` — слово/выражение + чипы перевода + Сохранить | Словарь подставляет переводы; секции: На изучении / Выучено / Стоп-лист (слова) |
| Стоп-лист на вкладке **Выражения** — пустое сообщение | В модели данных стоп-лист только для слов |
| SRS **не показывается** в таблице «На изучении» | SRS — внутренняя механика; данные в `word.srs` / `phrase.srs` |
| Источники в **попапе** (`sources-modal`), не колонкой | Экономия места на mobile; группировка: `formatSourcesGrouped()` — «Сериал - S01 - 01-07» |
| Слово **вручную** → кнопка ≡ активна; в попапе **«Добавлено вручную»** | `word.manual` или пустой `sources`; см. `sources-modal.js` |
| Колонки: **Слово \| Переводы \| 3 кнопки** (✓ выучено · ✕ удалить · ≡ источники) | Фиксированная таблица; кнопки — колонка на mobile, ряд на desktop |
| **Единые SVG-иконки** — `js/ui/action-icons.js` | ✓ выучено · ✕ удалить/убрать · 🚫 стоп-лист · ↩ вернуть · ≡ источники — одинаково в «На изучении», «Выучено», «Стоп-лист», импорте |
| Без тегов **изучаю / выучено / стоп** в строках | Вкладка уже задаёт контекст |
| Тег **ручное** — только для выражений, добавленных вручную | Слова без отдельного тега «в карточках» (убран как шум после импорта) |

### 2.4 Импорт

| Решение | Почему |
|---------|--------|
| Кнопка **«Загрузить новый файл»** по центру; hint **«srt или txt»** | Один вход для нового файла; без второй кнопки «другой файл» после коммита |
| **Авторазбор** сразу после загрузки | Без превью текста и без шага «Разобрать» |
| Метаданные .srt: **2 строки** — (Сериал+Сезон), (Название серии+Серия) | Компактнее на mobile |
| **Два шага** по словам: сначала **с переводом**, затем **без перевода** | Сначала быстрый импорт; без перевода — вручную или в стоп |
| Табы **Слова / Фразы** и **Список / Карточки** — на всю ширину; по умолчанию **Список** | Единый UX; список удобнее для длинных файлов |
| Кнопка **«Добавить выбранные в словарь»** — **над** табами | Не теряется при прокрутке длинного списка |
| В списке: **«Выбрать всё»** + **«Выбрано: N»** (только видимые на шаге) | Без фильтров «Новое/Без перевода» — шаг задаёт состав |
| Статистика «новых» в шапке — по **статусу** слова, не по галочке `included` | `analyzeSummary`: `!known && !stop && !studying && !removed` |
| Статистика файла: порядок **новых · знаете · изучаете · без перевода · стоп** | «Изучаете» — обращение к пользователю; блок «Слова с переводом» только на шаге импорта |
| На шаге «без перевода» — отдельный текст-подсказка, без общей сводки файла | Фокус на ручном вводе / стоп-листе |
| Попап подтверждения (**ОК / Поправить**) перед коммитом | Защита от случайного импорта |
| Сессия импорта: несколько коммитов → итог; мета-форма сбрасывается только в конце | Можно пройти оба шага одного файла без перезагрузки |
| Swipe-карточки + trans-chips; иконки ✓/🚫 в списке | Референс Lovable; те же иконки, что в Базе знаний |

### 2.5 Тренировка

| Решение | Почему |
|---------|--------|
| Без подсказки «1 шаг = 10 карточек…»; без заголовка «Последние шаги»; в истории **без колонки «Источник»** | Меньше шума; источник не нужен в сводке шагов |
| `STEP_SIZE = 10` | Короткие сессии; статистика шагов в **Тренировка**, не Настройки |
| Двойной SRS: **`enru`** и **`ruen`** независимо | EN→RU и RU→EN — разные навыки; см. `docs/agent-spec.md` §8 |
| «4 варианта»: промпт в карточке, варианты снаружи; **каждая строка — все переводы слова** (до 3, через « · ») | Верная строка и дистракторы; EN→RU и RU→EN |
| В режимах 1–2: ответ — **все переводы** из словаря, не один | `formatTranslations()` в `srs.js` |
| Setup: табы **на всю ширину**; направление **«Оба»** по умолчанию; режимы **1/2/3 — мультивыбор** (без «Смешанный») | Случайный режим из включённых; счётчик «к повторению» — в галочке |
| Карточка: **свайп** ← Не знаю / Знаю →; кнопки **Знаю / Не знаю**; **«исправить слово»** → попап (перевод, стоп, выучено) | `swipe-card.js`, `train-edit-modal.js` |
| Mobile-сессия: **без h1 «Тренировка»**; «Верно!/Не verno!» **на карточке под словом** (не под вариантами); компактные отступы карточки | Экран целиком на телефоне |
| Подготовка к серии/главе: `ctx.startPrepTraining()` | См. §4 — не импортировать router из views |

### 2.6 Данные и логика

| Решение | Почему |
|---------|--------|
| `stopList`: **`{ lemma, translations }`**, не строки | При удалении слова переводы сохраняются; миграция `normalizeStopList()` |
| Отображение слов/переводов: **`titleCase()`** в `js/core/display-text.js` | Единая заглавная буква в UI; данные в IndexedDB без изменения |
| HARD RESET: **два модала** (предупреждение → «ВЫ ТОЧНО…»); кнопки **столбцом на всю ширину**; Отмена/Нет — **`btn-dark`** (чёрные), Удалить/Да — красные | Отмена заметна; двойное подтверждение |
| Стоп-лист **пустой** при первом запуске | Пользователь сам наполняет; встроенный ~40 слов удалён |
| «Знаю» при импорте → карточка с переводами | Не только запись в `knowledge` |
| Лемматизация = **lookup** в `forms.json.gz` | Не обрезка суффиксов — см. spec §2 |
| IndexedDB + ручной .json | Без бэкенда; данные не синхронизируются между устройствами |

### 2.8 Сериалы и Книги

| Решение | Почему |
|---------|--------|
| Счётчик **N/M готово** — плашка у сериала/книги **и у каждой серии/главы** | Агрегат в шапке; детализация у эпизода |
| Без подзаголовка «Готовность = …» под заголовком страницы | Цифра уже в плашке; меньше шума на mobile |
| У серии/главы — **полоска** без текста + **две кнопки в ряд** | «Словарь серии/главы» · «Подготовка» |
| Прогресс по **снимку лексики** (`vocabulary` на ep/ch) | Слова «знаю» до импорта учитываются без `sources` |
| Снимок без карточки → `ensureSnapshotItems`: с переводом в изучение, иначе без перевода | Нет отдельной категории «новые» |
| **Готово** = выучено + стоп; **не готово** = на изучении + без перевода | Без перевода **не** входит в N/M готово |
| Бар: зелёный · серый (на изучении) · красный · жёлтый — **средняя насыщенность** (`--prog-*`) | Не тёмные акценты и не бледные `-bg` |
| **Словарь серии** — `source-vocab.js`, `ctx.openSourceVocab` | Фильтры с **счётчиками в кнопках**; sticky табы + шапка; плашка N/M справа от «← Назад»; редактирование переводов чипами |
| Подготовка — только **на изучении** с переводом (`isSnapshotPrepItem`) | `srs.js`, не только `sources` |

### 2.9 Отличия от Lovable (намеренные)

- Единый **База знаний** вместо отдельных Словарь / Фразы / Известные.
- **Сериалы** и **Книги** — отдельные пункты меню (не внутри одного раздела).

---

## 3. Ограничения (не нарушать)

- Без LLM / внешних API — всё оффлайн.
- Без бэкенда — статический хостинг.
- Vanilla JS, ES modules, **без сборщика**.
- Язык UI: **русский**.
- Отложено: PWA, облако, полный SM-2 — см. `docs/agent-spec.md` §10.

---

## 4. Подводные камни (технические)

| Проблема | Решение |
|----------|---------|
| Кнопка «Подготовка» не работает | **Не** `import router` из views → `ctx.startPrepTraining()` |
| Словарь серии не открывается | `ctx.openSourceVocab` / `ctx.closeSourceVocab` в `router.js`, не импорт router из views |
| Сломанный UI после правок | Bump `ASSET_VERSION` в `js/version.js` и `?v=` в `index.html`, `app.js`, `router.js`, imports |
| `bindTransChipsContainers` | Делегирование на root (WeakSet); bind модала один раз в `ensureKbAddModal` |
| Views импортируют `router.js` | Запрещено (дубль модуля, `appCtx = null`) |
| Прочерк в стоп-листе | `stopList` должен хранить `translations`; `repairStopListTranslations()` |
| Scroll-top не работает | Слушать `.content`, не `window` |
| Нет переводов на проде | В Git должны быть `data/dictionary.json.gz`, `data/forms.json.gz` |
| Сборка словаря | `node scripts/build-dictionary.mjs` (интернет один раз) |
| **Путь B:** серверные функции не появляются | Включаются только при заданном адресе сервера (Настройки → Аккаунт). Иначе — чистый оффлайн |
| **Путь B:** `.cursorignore` не создаётся агентом | Защищённый файл; создать вручную — содержимое в `docs/path-b-runbook.md` |
| **Путь B:** mixed-content при тесте | Фронт по HTTPS + бэк по `http://localhost` браузер блокирует; тестировать оба локально (8081 + 8000) |
| **Путь B:** синхронизация | Весь `state` — один JSON-блоб (last-write-wins по `updated_at`); `js/sync/sync.js` |

---

## 5. Структура проекта

```
serial-english/
├── index.html, agent-plan.md
├── docs/  agent-spec.md, agent-changelog.md, backend-spec.md, deploy-runbook.md, path-b-runbook.md
├── .nojekyll, netlify.toml, .github/workflows/deploy-pages.yml
├── css/styles.css
├── data/  dictionary.json.gz, forms.json.gz, phrases.json, translation-overrides.json
├── scripts/  build-dictionary.mjs
├── backend/                       ← Путь B: FastAPI (app/main, models, routers/, security, deps), Dockerfile, alembic/
└── js/
    ├── app.js, router.js          ← ctx.startPrepTraining, openSourceVocab, closeSourceVocab
    ├── api/  config.js, client.js  ← Путь B: адрес сервера/токен (localStorage), HTTP-клиент
    ├── sync/ sync.js               ← Путь B: синхронизация state ↔ сервер (push/pull/smart)
    ├── ui/  swipe-card, trans-chips, scroll-top, sources-modal, train-edit-modal, action-icons, version.js
    ├── db/database.js
    ├── import/  dictionary.js, phrases.js, library.js (опц. с сервера)
    ├── core/  analyzer, parser, lemmatizer, srs, readiness.js, display-text.js, …
    └── views/
        ├── catalog.js, import.js, knowledge.js, training.js
        ├── study-words.js, study-phrases.js
        ├── shows.js, books.js, source-vocab.js, settings.js, account-ui.js (Путь B)
```

Модалы в `index.html`: `#import-confirm-modal`, `#kb-add-modal` (добавление слова/выражения из Базы знаний).

---

## 6. Среды и ссылки

| | |
|---|---|
| Прод (РФ, без VPN) | https://manofvrhonor.github.io/serial-english/ |
| Netlify (запасной) | https://serialenglish.netlify.app/ (VPN из РФ) |
| Dev | http://localhost:8081 |
| Git | https://github.com/manofvrhonor/serial-english |
| Полное ТЗ | `docs/agent-spec.md` |
| История | `docs/agent-changelog.md` |

Деплой: `git push` в `main` → автообновление GitHub Pages.

---

## 7. Следующий шаг

**Путь B — сделано (ветка `dev`, локально):**
- ✅ Ветка `dev` + `.cursorignore`; код Пути B закоммичен (`bc87616`).
- ✅ Бэкенд (`uvicorn :8000`, SQLite) + фронт (`http.server 8081`); вход/регистрация — ОК.
- ✅ Синхронизация прогресса (round-trip `PUT`/`GET /api/progress`).
- ✅ Серверная библиотека: `POST /api/library/gravity-falls` (админ через `ADMIN_EMAILS`).
- ✅ Alembic init-миграция `3eb6b669eae6` (`15c9b74`); локальная `app.db` — `stamp head`.

**Путь B — осталось:**
1. (Опц.) UI-тест в браузере: тумблер «библиотека с сервера», пикер в Каталоге.
2. После полного теста на `dev` — merge в `main` (bump `?v=` при необходимости).
3. Деплой бэкенда в РФ — `docs/deploy-runbook.md` (Timeweb + Dockploy, занятия 6-7).

**Dev:** `python -m http.server 8081` (фронт) + `uvicorn app.main:app --reload --port 8000` (бэкенд).

Отложено: мобилка (Expo, занятия 2/5), PWA, полный SM-2 — см. план §5 и `docs/agent-spec.md` §10.
