# 📘 ТЗ / Инструкция для агента — проект «Serial English»

## 0. Как пользоваться этим документом
Это «карта проекта». Движемся по этапам (раздел 11). На каждом этапе сверяемся
с этим ТЗ, чтобы не терять контекст и конечную цель. ТЗ заморожено; менять —
только осознанно.

### 0.1 Быстрый старт новой сессии агента (минимум токенов)
1. Открыть проект `D:\VIBECODING\SERIAL ENGLISH`.
2. Прочитать **только разделы 12–14** этого файла (статус, структура, журнал).
3. Первое сообщение в чат: цель + `@agent-plan.md` + конкретная задача.
4. Не перечитывать весь agent-plan и не сканировать data/*.json — только нужные `js/` файлы.
5. Dev-сервер: `python -m http.server 8080`.

---

## 1. Цель проекта
Веб-приложение для изучения английских слов и выражений из СЕРИАЛОВ и КНИГ.
Пользователь загружает субтитры (.srt) или текст книги (.txt по главам),
приложение извлекает незнакомые слова и устойчивые выражения и тренирует их
по карточкам с интервальными повторениями. Фокус — подготовка к конкретной
СЕРИИ или ГЛАВЕ перед просмотром/чтением.

- Перевод тренируется в ОБЕ стороны: English → Russian и Russian → English.
- Язык интерфейса: РУССКИЙ.

---

## 2. Принципиальные решения (зафиксировано, не пересматриваем)
- Никакой LLM / внешних API. Всё работает оффлайн, локально.
- Лемматизация — НЕ обрезка суффиксов. Индекс форм `forms.json.gz` (словоформа → лемма),
  строится при сборке словаря из лемм + неправильных форм. В рантайме — только lookup.
  Сокращения (`it's`, `let's`, `there's`…) раскрываются в токенизацию.
- Переводы — оффлайн словарная база en→ru (используется и для обратного направления).
- Выражения — оффлайн готовая база + ручное добавление. Что не поймалось — не беда.
- Клиентское веб-приложение, без бэкенда. Прицел на PWA позже.
- Хранение: IndexedDB (основное) + ручной экспорт/импорт .json. Облако — позже.

---

## 3. Стек
Vanilla JS (ES modules), SPA без сборщика, IndexedDB, CSS.
Оффлайн-данные: `dictionary.json.gz` + `forms.json.gz` (сжатые JSON).
Сборка словаря: `node scripts/build-dictionary.mjs` (нужен интернет один раз).
Локальный сервер для разработки: `python -m http.server 8080`.

---

## 4. Разделы интерфейса
Боковое меню + рабочая область:
1. Импорт
2. Слова
3. Выражения
4. Тренировка
5. Сериалы (вложенность: Сериал → Сезон → Серия)
6. Книги (вложенность: Книга → Глава)
7. База знаний (выученное)
8. Настройки (интервалы SRS, стоп-лист, экспорт/импорт, HARD RESET)

---

## 5. Поток обработки импорта
Поддерживаются два типа источника:
- .srt → серия (внутри сезона, внутри сериала);
- .txt → глава (внутри книги).

Конвейер:
файл → парсинг → удаление стоп-листа (артикли, местоимения) → лемматизация
→ исключение слов из Базы знаний (по лемме)
→ поиск выражений по базе (целиком → в "Выражения"; отдельные слова выражения тоже идут в "Слова")
→ подстановка переводов из словарной базы
→ ЭКРАН ПОДТВЕРЖДЕНИЯ

Экран подтверждения показывает:
- новые слова (с переводами, чекбоксы);
- новые выражения (чекбоксы);
- слова БЕЗ перевода — пометка «нет перевода»: вписать вручную или исключить;
- пропущенные (уже известные) — счётчиком.

При импорте пользователь указывает, КУДА грузить:
- для .srt: Сериал → Сезон → Серия;
- для .txt: Книга → Глава.
Метки берутся из имени файла; при неудаче — задаются вручную.

Каждое слово помнит СПИСОК ИСТОЧНИКОВ (связь многие-ко-многим) — это могут
быть и серии, и главы книг одновременно.

---

## 6. Структура «Сериалы» и «Книги» + готовность
Сериалы: дерево Сериал → Сезон → Серия. У каждой серии — ПРОГРЕСС-БАР
готовности = выученные / все слова серии. Кнопка «Подготовка к просмотру» →
тренировка по неизученным словам серии. Агрегация готовности до сезона/сериала —
опционально (украшение).

Книги: дерево Книга → Глава. Полностью аналогично: загрузка .txt по главам,
словарь главы, прогресс-бар готовности, кнопка «Подготовка к чтению».

Слово, ушедшее в Базу знаний, повышает готовность всех источников, где оно
встречается (серий и глав).

---

## 7. Разделы «Слова» и «Выражения»
Структура идентична. Поля элемента:
- лемма / фраза;
- переводы (топ-3);
- ДВА блока SRS — EN→RU и RU→EN (уровень, галочки, дата показа у каждого);
- список источников (серии / главы);
- статус «выучено».

Действия: редактировать переводы и их порядок (топ-3), удалить, пометить
выученным (→ База знаний), добавить вручную, поиск/фильтр.

Переводы (топ-3): при добавлении пользователь видит полный список значений
из словарной базы и выбирает ДО 3, расставляя приоритет (топ-1, топ-2, топ-3).

---

## 8. Тренировка и SRS

### 8.1 Режимы предъявления
1. Слово + перевод (показано сразу) → «Знал / Не знал».
2. Перевод по клику (скрыт, раскрывается) → «Знал / Не знал».
3. 4 варианта (выбор).
4. МИКС — каждая карточка случайно в одном из трёх режимов.

### 8.2 Направление перевода
В тренировке выбирается направление:
- EN→RU: показывается английское слово, подбирается русский перевод.
- RU→EN: показывается русское слово, подбирается английское.
- Оба — случайно чередуются.

Режим «4 варианта» работает в обе стороны:
- EN→RU — варианты русские переводы;
- RU→EN — варианты английские слова.
Правило: среди 4 вариантов ТОЛЬКО ОДИН правильный. Правильный берётся из
топ-1/2/3 с весами: топ-1 чаще, топ-2 средне, топ-3 реже. 3 неправильных —
из переводов/слов других элементов базы.

### 8.3 SRS — два независимых направления
У каждого слова ДВА независимых блока SRS: enru и ruen. У каждого:
- уровень 0 → 5, интервалы [1, 3, 7, 16, 30] дней (уровень 0 = сегодня);
- 3 ГАЛОЧКИ по режимам (по одной на режим 1/2/3);
- своя дата следующего показа.

Логика (для каждого направления отдельно):
- верный ответ в режиме → ставит галочку этого режима В ДАННОМ направлении;
- собраны все 3 галочки направления → уровень этого направления +1, новый
  интервал, галочки обнуляются;
- ошибка → уровень этого направления -1 (не ниже 0; НА 0 ОСТАЁТСЯ 0),
  галочки направления сброшены, показ — скоро.

Слово может иметь, например, EN→RU на уровне 5, а RU→EN на уровне 1 — это
нормально и ожидаемо.

ВЫУЧЕНО (→ База знаний): когда ОБА направления достигли максимального
уровня (5) с собранными галочками.

В сессию берутся карточки/направления с датой показа ≤ сегодня. В МИКС режим
карточки = тот, в котором её показали (двигает соответствующую галочку).

### 8.4 Старт тренировки
Выбор: что тренируем (Слова / Выражения / Всё), направление (EN→RU / RU→EN / Оба),
режим (1/2/3/МИКС), «только к повторению сегодня» (вкл/выкл). Плюс отдельный вход
из «Сериалов» / «Книг» (подготовка к серии/главе).

**Шаги (реализовано):** 1 шаг = 10 карточек. После шага — короткая статистика,
кнопка «Следующий шаг →». Статистика «Сегодня» и история шагов — в разделе «Тренировка»,
не в «Настройках». Константа `STEP_SIZE = 10` в `js/views/training.js`.

---

## 9. Модель данных (черновик json, финал на Этапе 2)
Этот формат = формат экспорта/импорта. Спроектирован с запасом под будущую синхронизацию.

{
  "version": 2,
  "settings": {
    "intervals": [1,3,7,16,30],
    "stopList": ["the","a","..."],
    "sessionHistory": [],
    "lastSession": null
  },
  "shows": [
    {
      "id": "show_friends", "title": "Friends",
      "seasons": [
        { "id": "s01", "title": "Season 1",
          "episodes": [ { "id": "S01E07", "title": "S01E07", "wordIds": ["w_run"] } ] }
      ]
    }
  ],
  "books": [
    {
      "id": "book_hobbit", "title": "The Hobbit",
      "chapters": [ { "id": "ch01", "title": "Chapter 1", "wordIds": ["w_run"] } ]
    }
  ],
  "words": [
    {
      "id": "w_run",
      "lemma": "run",
      "translations": ["бежать", "управлять", "запускать"],
      "sources": ["S01E07", "ch01"],
      "srs": {
        "enru": { "level": 2, "checks": { "mode1": true,  "mode2": false, "mode3": false }, "due": "2025-06-01" },
        "ruen": { "level": 0, "checks": { "mode1": false, "mode2": false, "mode3": false }, "due": "2025-06-01" }
      },
      "learned": false
    }
  ],
  "phrases": [
    {
      "id": "p_give_up", "text": "give up",
      "translations": ["сдаться", "бросить"],
      "sources": ["S01E07"],
      "srs": {
        "enru": { "level": 0, "checks": { "mode1": false, "mode2": false, "mode3": false }, "due": "2025-06-01" },
        "ruen": { "level": 0, "checks": { "mode1": false, "mode2": false, "mode3": false }, "due": "2025-06-01" }
      },
      "learned": false, "manual": false
    }
  ],
  "knowledge": { "wordLemmas": ["dog","cat"], "phrases": ["break the ice"] }
}

---

## 10. Что НЕ входит в MVP (отложено)
- Облачная синхронизация (только ручной экспорт/импорт .json).
- Полный SM-2 (используем минимальный SRS).
- PWA-обёртка (архитектурно готовим, оформляем позже).
- Любой LLM / API — не планируется вообще.

---

## 11. Поэтапный план реализации
Идём строго по этапам, каждый завершаем рабочим результатом.

1. Каркас. Стек, структура проекта, навигация (все разделы пустыми, с
   вложенностью Сериалы/Книги), настройка IndexedDB, экспорт/импорт пустого json.
2. Модель данных. Финал json-схемы (два направления SRS, деревья shows/books),
   чтение/запись IndexedDB, реальный экспорт/импорт.
3. Импорт. Парсинг .srt и .txt, стоп-лист, лемматизация, словарь, выражения,
   экран подтверждения, привязка к серии/главе.
4. Слова / Выражения. Списки, топ-3 переводы, ручное добавление, «выучено», поиск.
5. Тренировка + SRS. 3 режима + МИКС, оба направления, двойной SRS с
   галочками и откатом, сессии «к повторению».
6. Сериалы и Книги. Деревья, прогресс-бары готовности, «Подготовка к просмотру/чтению».
7. База знаний. Список выученного, возврат в изучение, исключение из импорта.
8. Полировка. Настройки, стоп-лист UI, сводки сессий, UX.


---


## 12. Выполненные и текущие этапы

### ГОТОВО

Этап 1: Каркас
Цель Этапа 1 (что будет работать в конце)
Открывается index.html — видно боковое меню со всеми 8 разделами.
Клик по пункту меню показывает соответствующий раздел (пока — просто заголовок-заглушка).
IndexedDB создаётся при первом запуске.
В «Настройках» работают кнопки Экспорт (скачивает .json) и Импорт (загружает .json) — пока на пустых данных.


Этап 2: Модель данных
Цель Этапа 2 (что будет работать в конце)
Финализирована json-схема (версия 2): два направления SRS (EN→RU и RU→EN), деревья shows (Сериал→Сезон→Серия) и books (Книга→Глава).
В IndexedDB лежит структурированное состояние по этой схеме.
Реализованы чтение/запись по схеме, реальный экспорт/импорт с проверкой структуры (битый/чужой json отклоняется).
Есть служебная функция миграции/нормализации состояния под актуальную версию схемы.


Этап 3: Импорт — **ГОТОВО** (см. цели в разделе 11, п.3).
Этап 4: Слова / Выражения — **ГОТОВО** (см. цели в разделе 11, п.4).

Этап 5: Тренировка + SRS — **ГОТОВО**
- SRS: `{ level, checks, due }`; 3 режима + МИКС; оба направления.
- **Шаги:** 10 карточек → «Шаг N завершён» → «Следующий шаг»; `STEP_SIZE=10` в training.js.
- Статистика «Сегодня» + таблица «Последние шаги» — в разделе **Тренировка**.

Этап 6: Сериалы и Книги — **ГОТОВО**
- Деревья, прогресс-бары, «Подготовка» → `router.startPrepTraining()`.

Этап 7: База знаний — **ГОТОВО**
- Выученное; возврат в изучение; стоп-лист / исключение из импорта.

Этап 8: Полировка — **ГОТОВО** → **MVP функционально завершён**
- Настройки: интервалы SRS, стоп-лист UI, экспорт/импорт, **HARD RESET**.
- HARD RESET → `hardResetState()`: очищает пользовательские данные; **дефолтный**
  стоп-лист и интервалы SRS остаются (см. раздел 14).
- `ctx.reload()` после импорта .json.


### СЛЕДУЮЩАЯ ФАЗА

**Этап 9: UI / дизайн (Lovable) — В РАБОТЕ**

Прототип (референс): `D:\VIBECODING\word-weaver-offline` (GitHub: manofvrhonor/word-weaver-offline).
Основной проект на GitHub: **manofvrhonor/serial-english**.

**Сделано (сессия 2026-06):**
- Дизайн-токены oklch, новый каркас: sidebar + mobile nav + header (`index.html`, `css/styles.css`, `router.js`).
- **Импорт:** вкладки Слова/Фразы, режимы Карточки/Список, swipe-стек (`js/ui/swipe-card.js`).
- **Тренировка (настройки):** segment-кнопки + сетка режимов 2×2 как в Lovable (`training.js`).
- **Переводы-чипы:** drag-порядок, × удалить, + добавить — `js/ui/trans-chips.js`; в **Слова**, **Выражения**, **Импорт**.
- **Удаление слова** (✕ в «Слова») → лемма в **стоп-лист** (`deleteWord()` → `addStopWord()`).
- Исправлен баг: кнопки удаления не работали (неверный селектор `.row-btn`).
- **Настройки:** вертикальный stack карточек (`settings-stack`), max-width как в Lovable.
- **База знаний:** вкладки Слова/Выражения, таблица в `card list-card`, icon-кнопки действий.
- **Сериалы / Книги:** card-layout (`source-card`, badge, grid эпизодов/глав) как в Lovable `sources.tsx`.

**Осталось по Этапу 9:**
- Сессия тренировки (карточка во время шагов) — довести до прототипа.
- Финальная сверка экранов с http://localhost:8080 (Lovable) vs :8081 (наш).

Бизнес-логику (`js/core`, `js/db`, `js/import`) не переписывать без необходимости.


---

## 13. Структура проекта

serial-english/
│
├── index.html
├── agent-plan.md           ← эта карта (читать агенту в начале сессии)
│
├── css/
│   └── styles.css
│
├── data/
│   ├── dictionary.json.gz  ← 30k лемм → [до 3 переводов] (~500 КБ)
│   ├── forms.json.gz       ← словоформа → лемма (~540 КБ)
│   ├── phrases.json        ← офлайн-база выражений EN→RU
│   └── translation-overrides.json  ← ручные приоритеты переводов (~130 слов)
│
├── scripts/
│   ├── build-dictionary.mjs   ← сборка dictionary + forms (нужен node + сеть)
│   └── test-lemmatizer.mjs     ← smoke-test lookup
│
└── js/
    ├── app.js
    ├── router.js
    ├── ui/
    │   ├── swipe-card.js       ← swipe при импорте (Этап 9)
    │   └── trans-chips.js      ← чипы переводов: порядок, удаление (Этап 9)
    ├── db/
    │   └── database.js     ← IndexedDB, схема v2, экспорт/импорт, хелперы
    ├── import/
    │   ├── dictionary.js   ← загрузка dictionary.gz + forms.gz
    │   └── phrases.js      ← загрузка phrases.json
    ├── core/
    │   ├── parser.js
    │   ├── analyzer.js
    │   ├── lemmatizer.js
    │   ├── inflections.js      ← сборка словаря
    │   ├── srs.js              ← SRS, buildSession, applyAnswer
    │   └── readiness.js        ← прогресс серий/глав
    └── views/
        ├── import.js
        ├── words.js
        ├── phrases.js
        ├── training.js           ← шаги по 10, статистика «Сегодня»
        ├── shows.js
        ├── books.js
        ├── knowledge.js
        └── settings.js           ← интервалы, стоп-лист, HARD RESET

---

## 14. Журнал решений и известные расхождения

### Словарь и формы (сессия 2025-06)
- Источники переводов при сборке: iuzhakov → spishniak; `translation-overrides.json` — финальный приоритет.
- Эвристики качества: -ity/-tion → существительные, когнаты (grav→грав), фильтр падежных дублей.
- Пересборка: `node scripts/build-dictionary.mjs`
- Плохие переводы править в `data/translation-overrides.json`, затем пересобрать.

### SRS-схема — выровнена (Этап 5)
Формат: `{ level, checks: {mode1,mode2,mode3}, due }` (ISO-дата).
Миграция из `{ box, due, history }` в `normalizeSrsSide()`.

### Тренировка: шаги (сессия 2025-06)
- 1 шаг = 10 карточек (`STEP_SIZE` в `training.js`).
- Очередь строится один раз при «Начать»; шаги — срезы очереди.
- `settings.sessionHistory` — одна запись на каждый завершённый шаг.
- `getTodayTrainingSummary()` — агрегация шагов за сегодня.

### HARD RESET (сессия 2025-06)
`hardResetState()` в `database.js` сбрасывает: words, phrases, shows, books,
knowledge, sessionHistory. **Не трогает:** офлайн-файлы data/*.json(.gz).
**Остаётся в IndexedDB:** дефолтные intervals [1,3,7,16,30] и встроенный stopList
(~40 служебных слов для фильтра импорта) — это не «мусор пользователя», а настройка
проекта. Пользовательские дополнения в stopList тоже сбрасываются к дефолту.

### Запуск dev
Рабочая папка: `D:\VIBECODING\SERIAL ENGLISH`

| Приложение | Команда | URL |
|---|---|---|
| Serial English | `python -m http.server 8081` | http://localhost:8081 |
| Lovable-прототип | `npm run dev` в `word-weaver-offline` | http://localhost:8080 |

Git: https://github.com/manofvrhonor/serial-english

### Этап 9 — UI (сессия 2026-06)
- Референс Lovable: `D:\VIBECODING\word-weaver-offline`.
- `js/ui/trans-chips.js` — до 3 переводов; drag = порядок топ-1/2/3; используется в words, phrases, import.
- `deleteWord()` добавляет лемму в `settings.stopList` (не предлагать при импорте).
- Импорт: swipe ← стоп, → знаю, ↑ к импорту; список с чипами переводов.

### Следующий шаг агента
**Продолжить Этап 9.** Прочитать agent-plan.md (разделы 12–14).
Довести UI сессии тренировки (карточка во время шагов) до Lovable; финальная сверка.
Логику (`js/core`, `js/db`, `js/import`) не ломать.
Прикрепить к чату: `@agent-plan.md` + конкретный экран/задача.