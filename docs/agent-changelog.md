# 📜 Журнал изменений — Serial English

История сессий и коммитов. Читать при разборе «когда и зачем сделали».
**Не дублировать** в `agent-plan.md` — дописывать сюда.

---

## Сессия 2026-06-26 — Путь B: бэкенд, аккаунты, синхронизация (НЕ закоммичено)

Старт перехода с оффлайн-статики на серверную версию (аккаунты + облачная
синхронизация прогресса + серверная библиотека). Roadmap — план `serial_english_path_b`;
методика — занятия 2-7. Документы: `docs/backend-spec.md`, `docs/deploy-runbook.md`,
`docs/path-b-runbook.md`.

**Бэкенд (новое, `backend/`):** FastAPI + SQLAlchemy 2.0 + JWT (bcrypt). Эндпоинты
`/api/auth/{register,login,me}`, `GET/PUT /api/progress` (снимок всего `state` одним
JSON, last-write-wins), `GET/POST /api/library/{id}` (формат `data/library/*.json`),
`/health`. SQLite→PostgreSQL, секреты в `.env`. Docker (`Dockerfile`, `docker-compose.yml`),
Alembic-скелет, `.env.example`, `backend/README.md`.

**Фронтенд (аддитивно, оффлайн не сломан):** новые модули `js/api/config.js`,
`js/api/client.js`, `js/sync/sync.js`, `js/views/account-ui.js`. В Настройках —
секция «Аккаунт и синхронизация» (вход/регистрация, Синхронизировать/Выгрузить/Загрузить,
тумблер серверной библиотеки). `js/import/library.js` — опционально грузит библиотеку
с сервера (fallback на локальные файлы). Без адреса сервера приложение работает как раньше.

**Версия:** bump `ASSET_VERSION` → `20260674`; обновлены `?v=` у изменённых файлов
(`index.html`, `app.js`, `router.js`, `import.js`).

**Фикс (локальный тест):** регистрация падала с 501 — адрес сервера был сохранён с
обратным слэшем (`http:\localhost:8000`), браузер трактовал его как относительный путь
и слал POST на статику (8081). Добавлен `normalizeApiBase()` в `js/api/config.js`
(чинит обратные слэши, один слэш после схемы, отсутствие схемы, хвостовые слэши;
нормализует и при чтении — самочинит уже сохранённые значения). Bump → `20260675`.

**Локальный тест Пути B (пройден, закоммичено в `dev`):**
- Ветка `dev` создана/запушена; `.cursorignore` добавлен; код Пути B закоммичен (`bc87616`).
- Бэкенд поднят (venv + `uvicorn :8000`, SQLite); регистрация/вход — ОК (вживую).
- Синхронизация прогресса: round-trip `PUT`/`GET /api/progress` подтверждён.
- Серверная библиотека: `POST /api/library/gravity-falls` (админ `probe@gmail.com`
  через `ADMIN_EMAILS` в `.env`); `GET /api/library` отдаёт сериал (1 сезон, 20 серий).
- Alembic: сгенерирована init-миграция `3eb6b669eae6` (users/progress/shows),
  проверена на чистой БД, локальная `app.db` — `stamp head`; закоммичено (`15c9b74`).

**Следующее:** UI-тест синхронизации/библиотеки в браузере (по желанию); затем merge
`dev`→`main` после полного теста; деплой бэкенда по `deploy-runbook.md`.

**НЕ сделано (среда сессии):** ветка `dev` и `.cursorignore` — терминал не выполнял
команды, `.cursorignore` защищён от записи агентом → команды/содержимое в
`docs/path-b-runbook.md`. Изменения **не закоммичены** (прод на `main` не затронут,
остаётся `?v=20260673`).

**Следующее:** создать `dev`, протестировать бэкенд+фронт локально (`path-b-runbook.md`),
затем merge в `main`; деплой по `deploy-runbook.md`. Мобилка — отложена.

---

## Сессия 2026-06-23 — mobile polish: тренировка, слова, HARD RESET (закрыта)

**Каталог (`catalog.js`, `router.js`, nav):** единый раздел «Каталог» (импорт + сериалы + книги) — в `main` с коммита `a22805a`.

**Тренировка (`training.js`, `styles.css`):** mobile — без h1 «Тренировка» в сессии; «Верно!/Не verno!» на карточке под словом; компактные отступы карточки.

**Отображение слов (`display-text.js`, `srs.js`, `trans-chips.js`, views):** `titleCase()` — единая заглавная буква в UI (карточки, варианты, словарь, чипы); данные в БД не меняются.

**HARD RESET (`settings.js`, `styles.css`):** кнопки столбцом на всю ширину; второй модал «ВЫ ТОЧНО ХОТИТЕ ВСЁ УДАЛИТЬ?» (Нет/Да); Отмена и Нет — чёрные `btn-dark`.

**Версия:** `?v=20260673`. **Деплой:** commit + push в `main`.

---

## Сессия 2026-06-23 — источники, polish тренировки (закрыта)

**Источники (`database.js`, `library.js`, `readiness.js`, `sources-modal.js`):** при повторном импорте дописывается `sources` у существующих карточек; `repairSourcesFromVocabulary` при загрузке; в попапе группировка «Сериал - S01 - 01-07».

**Тренировка (`training.js`, `srs.js`, `train-edit-modal.js`, `styles.css`):** setup — табы на всю ширину, «Оба» по умолчанию, мультивыбор режимов 1/2/3; счётчик due в галочке. Сессия — свайп ←/→, «Знаю»/«Не знаю», «исправить слово/выражение» (перевод, стоп, выучено).

**Версия:** `?v=20260661`. **Деплой:** commit + push в `main`.

---

## Сессия 2026-06-23 — библиотека, админ-курирование, пикер (закрыта)

**Библиотека (`js/import/library.js`, `import.js`, `data/library/`):** загрузка `index.json` + `<id>.json`; `importLibraryEpisodes` с переводами из набора; кнопка «Выбрать из библиотеки» — пикер сериал/сезон/серии, «весь сезон/сериал», сводка после импорта. Тестовый набор **Gravity Falls** (`gravity-falls.json`). Dev только через HTTP-сервер (`file://` не грузит JSON).

**Админ-курирование (`admin-library.js`):** глобальные правки переводов/удалений; режим «Все слова сезона»; очереди «На обработку / Готово»; массово «Готово для всех с переводом» и «Пропустить все без перевода».

**База знаний:** убран тег **«в карточках»** в «Выучено»/«Стоп» (шум после массового импорта).

**Версия:** `?v=20260655`. **Деплой:** commit + push в `main`.

---

## Сессия 2026-06-23 — админ-библиотека, фазы 1–2 (закрыта)

**Админ-гейт:** `admin-gate.js`, тумблер + модальный пароль в Настройках, роут `library-admin`.

**Админ-инструмент:** `zip.js` (native inflate), `admin-library.js` — zip → parseSrt → analyze → курирование (чипы, удаление, +ещё zip), черновик `localStorage`, экспорт show.json / index.json, «Новый набор» / «Удалить» без потери черновика.

**UI админки (итерации):** компактный список серий + EDIT NAME (модал); тулбар 2 строки; длинный список слов без внутреннего скролла; сохранение scroll при удалении слова.

**Прочее:** подстановка переводов из словаря **2** (`SUGGEST_TRANS_LIMIT`); интервалы SRS в одну строку; настройки без лишнего подзаголовка.

**Версия:** `?v=20260653`. **Деплой:** не push'или — commit + push в следующей сессии.

**Следующее:** `library.js`, пикер на «Импорте», тестовый набор в `data/library/`.

---

## Сессия 2026-06-23 — нижняя навигация + план библиотеки/админки (закрыта)

**Нижняя навигация (`styles.css`):** вся панель серого фона; иконки одного цвета (`--primary`); **активный таб** — белый фон + лёгкая тень (вместо просто тёмной иконки).

**Версия:** CSS `?v=20260640` (`version.js`, `index.html`). JS-каскад добьём при следующих правках.

**Запланировано (реализация в следующей сессии):** фича «Библиотека сериалов + админ-авторинг» — полный план в `docs/feature-library-admin-plan.md`; «Следующий шаг» в `agent-plan.md` §7. Кратко: выбор готовых сериалов на «Импорте» + админ-режим (тумблер в Настройках, пароль `54321`) с загрузкой zip → чистка → курирование → экспорт в `data/library/`.

**Деплой:** push в `main`.

---

## Сессия 2026-06-22 — UI фильтров, прогресс-бары, тренировки (закрыта)

**База знаний / словарь серии (`knowledge.js`, `source-vocab.js`, `styles.css`):**
- Верхние теги статистики убраны; **счётчики в кнопках фильтра** (подпись + число 2× крупнее).
- Кнопка фильтра «Без перевода» (полное слово).

**Сериалы (`styles.css`, `readiness.js`):** прогресс-бары — цвета статусов со **средней насыщенностью** (`--prog-*`).

**Импорт (`import.js`):** порядок сводки **новых · знаете · изучаете · без перевода · стоп**; «изучаете» вместо «изучать»; убрана путаница «0 без перевода» после завершения импорта.

**Тренировка (`srs.js`, `training.js`):** на карточках и в «4 варианта» показываются **все переводы** слова (до 3); каждая строка вариантов — полный набор переводов дистрактора.

**Версия:** `?v=20260639`. **Деплой:** push в `main`.

---

## Сессия 2026-06-21 — UX словаря серии/главы (закрыта)

**Словарь (`source-vocab.js`, `styles.css`):**
- Убрана колонка **«Статус»**; фильтры по цветным табам (без «Все»); по умолчанию «На изучении».
- Статистика: плашка **N/M готово** + цветные теги (первым «на изучении»); убраны подзаголовок «Словарь серии» и дублирующая строка текста.
- Переводы занимают ширину бывшей колонки статуса; кнопки ✓/стоп — квадратные, **в одну строку**.
- **Sticky:** при скролле зафиксированы табы Слова/Выражения, табы статусов и шапка «Слово / Переводы».

**База знаний (`knowledge.js`):** вкладки На изучении / Выучено / Стоп-лист раскрашены по статусу.

**Готовность (`readiness.js`):** сегмент «на изучении» на прогресс-баре (серый).

**Версия:** `?v=20260628`. **Деплой:** push в `main`.

---

## Сессия 2026-06-19 — полировка P3 + UX (закрыта)

**База знаний (`knowledge.js`):** счётчики в шапке обновляются при ✓/стоп на «На изучении» (`refreshKbStats`).

**Импорт (`import.js`):** при файле «всё знакомо» — полная сводка по файлу + пояснение; вкладка показывает total, не 0.

**Сериалы/книги (`shows.js`, `books.js`, `source-vocab.js`, `router.js`):**
- Плашка **N/M готово** у каждой серии/главы; кнопки **Словарь** и **Подготовка** в один ряд.
- Экран словаря источника: фильтры выучено · на изучении · стоп · без перевода.
- `ensureSnapshotItems` — слова из снимка без карточки → в изучение (с переводом) или без перевода; категории «новые» нет.

**Готовность (`readiness.js`):** **готово = выучено + стоп**; без перевода и на изучении — не готово.

**Версия:** `?v=20260726`. **Деплой:** не выполнен.

---

## Сессия 2026-06-19 — P3 прогресс серий/глав

**Снимок лексики (`database.js`, `import.js`):**
- На эпизод/главу: `vocabulary: { words, phrases }` — при разборе и коммите.
- Повторный импорт перезаписывает снимок.

**Готовность (`readiness.js`, `shows.js`, `books.js`):**
- Прогресс = снимок × глобальный статус; многоцветный бар; legacy — по `sources`.

**Подготовка (`srs.js`):** `isSnapshotPrepItem` по снимку.

**Версия:** `?v=20260722` → далее 20260726.

---

## Этапы 1–8 (MVP) — выполнены

| Этап | Статус | Ключевое |
|------|--------|----------|
| 1 Каркас | ✓ | Меню, IndexedDB, экспорт/импорт |
| 2 Модель данных | ✓ | json v2, shows/books, миграции |
| 3 Импорт | ✓ | .srt/.txt, лемматизация, подтверждение |
| 4 Слова/Выражения | ✓ | топ-3, выучено, поиск |
| 5 Тренировка+SRS | ✓ | 3 режима+МИКС, STEP_SIZE=10 |
| 6 Сериалы/Книги | ✓ | деревья, готовность, подготовка |
| 7 База знаний | ✓ | выученное, стоп-лист |
| 8 Полировка | ✓ | HARD RESET, настройки |

---

## Сессия 2026-06 — UI Lovable (этап 9)

- Дизайн-токены oklch; sidebar + mobile nav.
- Импорт: вкладки Слова/Фразы, Карточки/Список, swipe (`js/ui/swipe-card.js`).
- Тренировка: segment-кнопки, сетка режимов 2×2.
- Переводы-чипы (`js/ui/trans-chips.js`).
- Card-layout: Настройки, База знаний, Сериалы, Книги.

Референс: `D:\VIBECODING\word-weaver-offline` (manofvrhonor/word-weaver-offline).

---

## Сессия 2026-06 — UX / навигация

- **База знаний** — единый хаб: База / На изучении / Выучено / Стоп-лист.
- Панели: `study-words.js`, `study-phrases.js`.
- Из Настроек убраны стоп-лист и блок «База».
- HARD RESET: модальный попап (`#import-confirm-modal` в `index.html`).
- Импорт: попап ОК / Поправить; сброс формы после коммита.
- Cache-bust ES-модулей: `?v=…` в router и index.html.

---

## Сессия 2026-06 — тренировка, импорт, стоп-лист

- Карточка: badges режим/прогресс/направление; «4 варианта» — промпт в карточке.
- После «Разобрать»: статистика файла; в списке только **новые** (`analyzer.js`).
- Стоп-лист: пустой при старте; встроенный ~40 слов **удалён**; `normalizeStopList()`.
- «Знаю» при импорте → карточка с переводами (`addKnownWordFromImport`).
- UI: «Слово» вместо «Лемма».

---

## Сессия 2026-06 — UX полировка (закрытие этапа 9)

**Mobile:** 6 кнопок в nav (без «Ещё»/sheet); нет `mobile-header`; `h1` скрыт на mobile.

**Импорт:** Сезон+Серия в одну строку (`input-num`).

**Scroll:** `js/ui/scroll-top.js`; скролл в `.content`, не window (`100dvh` app-shell).

**Таблица «На изучении»:** без колонок SRS/Источники; 3 кнопки (✓/✕/≡); sources-modal.

**Сверка Lovable:** desktop + mobile 390px — все экраны пройдены.

---

## Сессия 2026-06 — пост-релиз

- **Prep fix:** `ctx.startPrepTraining()` в `initRouter`; views не импортируют router.
- **Стоп-лист:** `{ lemma, translations }`; `repairStopListTranslations()`.
- **Desktop shell:** убран `desktop-header`; toggle в `.sidebar-footer`.
- **Теги:** убраны изучаю/выучено/стоп из строк (контекст = вкладка).

---

## Сессия 2026-06 — публикация

| Среда | URL | Примечание |
|-------|-----|------------|
| GitHub Pages | https://manofvrhonor.github.io/serial-english/ | Основной, без VPN в РФ |
| Netlify | https://serialenglish.netlify.app/ | Запасной; из РФ нужен VPN |
| Локально | http://localhost:8081 | `python -m http.server 8081` |

- В Git обязательны `data/dictionary.json.gz`, `data/forms.json.gz`.
- `.nojekyll`, `.github/workflows/deploy-pages.yml`, `netlify.toml`.
- Данные пользователя — IndexedDB (не синхронизируются между устройствами).

---

## Сессия 2026-06-17 — импорт и иконки (закрыта)

**Импорт (`import.js`, `analyzer.js`):**
- Метаданные .srt: 2 строки (Сериал+Сезон · Название серии+Серия).
- Авторазбор после загрузки — без превью и кнопки «Разобрать».
- Двухшаговый поток: **слова с переводом** → **слова без перевода** (то же для фраз); `wordsPhase` / `phrasesPhase`.
- Кнопка «Добавить выбранные в словарь» над табами; табы Слова/Фразы и Список/Карточки на всю ширину; по умолчанию Список.
- Список: «Выбрать всё» + «Выбрано: N» (только видимые на текущем шаге).
- `analyzeSummary.newCount` — по статусу слова, не по `included`.
- Кнопка «Загрузить новый файл»; убрана «Загрузить другой файл» после коммита.
- Несколько коммитов за одну сессию файла → накопительный итог.

**Иконки (`js/ui/action-icons.js`):**
- Единые SVG: ✓ выучено · ✕ удалить · 🚫 стоп · ↩ вернуть · ≡ источники.
- Подключены: `study-words.js`, `study-phrases.js`, `knowledge.js` (Выучено/Стоп), `import.js`.

**Документация:** обновлены `agent-plan.md` §2.3–2.4, `agent-spec.md` §5.

**Деплой:** не выполнен; перед push — bump `?v=20260718`.

---

## Сессия 2026-06-19 — иконки, стоп-лист, счётчики (закрыта)

**Сделано:**
- `js/ui/action-icons.js` — единые кнопки: 🚫 стоп-лист, 🗑 удалить, ✓ выучено, ↩ вернуть.
- Стоп-лист: крестик на «На изучении» → в стоп; в стоп-листе → полное удаление (`purgeWord`); возврат сохраняет `sources`.
- Счётчики: `countTrainingItems` вместо удвоения EN/RU; `isTrainableItem` в статистике Базы.
- Сериалы/Книги: fix прогресс-бара подготовки (CSS `prog-bar-thin`).

**Аудит:** полный план ревизии (P0/P1/P2) — слова без перевода, фокус поиска, `?v=`, мёртвый код. **Реализация отложена.**

**Git:** `a95f023` → `main` → GitHub Pages.

---

## Сессия 2026-06-19 — P0 ревизии (закрыта)

**База знаний (`knowledge.js`, `study-words.js`, `study-phrases.js`, `database.js`):**
- Статистика: **в работе** (все на изучении) и **к повторению сегодня** (`countTrainingItems`) вместо одной подписи «к повторению».
- Фильтр **«Без перевода (N)»** на вкладке «На изучении» при N > 0.
- Поиск в Выучено/Стоп-лист: обновление только `#kb-content` — фокус в поле поиска не теряется.

**Документация:** `agent-plan.md` §2.3, §7; `agent-spec.md` §7.

**Деплой:** не выполнен; перед push — `?v=20260721`.

---

## Сессия 2026-06-19 — P2 ревизии (закрыта)

**Тренировка (`srs.js`, `training.js`):**
- Дистракторы «4 варианта»: по одному ответу от других элементов; без заглушек; `canUseChoiceMode`; fallback на режим 2.
- Очередь: `shuffleArray` + `shuffleAvoidingAdjacent` — одно слово не идёт дважды подряд при «Оба направления».

**a11y:** `aria-label` на кнопках-иконках, вариантах ответа и действиях тренировки; `:focus-visible` на кнопках и nav.

**Версия:** `?v=20260721`.

---

## Сессия 2026-06-19 — закрытие (коммит + push)

**В релиз:** P0 (база знаний) + P1 (версии, мёртвый код, импорт) + P2 (тренировка, a11y). Asset `?v=20260721`.

**Отложено → P3:** прогресс серий/книг по **снимку лексики файла** + глобальный статус слова (не только `sources` на карточках). См. `agent-plan.md` §7.

---

## Сессия 2026-06-19 — P1 ревизии (закрыта)

**Cache-bust:** единый `?v=20260720` в `index.html`, `app.js`, `router.js` и вложенных import; `js/version.js` — источник версии.

**Мёртвый код:** удалены `countDue`, `stripBuiltinStopWords`, `views/words.js`, `views/phrases.js`.

**Тренировка (`srs.js`, `training.js`):** единые `TRAINING_MODE_LABELS` / `HINTS` / `ORDER` — setup и badge в сессии совпадают со spec §8.1.

**Импорт (`import.js`, `router.js`):** после коммита — реальный счётчик `noTrans` из `getAppStats`; кнопки «Дополнить переводы» / «К тренировке» через `ctx.navigateTo`.

---

## Сессия 2026-06-17 — итог (закрыта)

**База знаний:** переключатель Слова/Выражения сверху; статистика без дублей; поиск + «Добавить» → модал `#kb-add-modal`; унификация добавления на всех вкладках.

**Импорт / Тренировка:** компактнее (кнопка «Загрузить файл», без лишних подсказок и колонки «Источник»).

**trans-chips:** fix ✕ — делегирование событий; bind модала один раз.

**Ручные слова:** `word.manual`; кнопка ≡ → «Добавлено вручную».

**Сериалы / Книги:** N/M изучено только в плашке шапки; без подзаголовка «Готовность = …» и без дублей у серий/глав.

**Git:** `8107f1f` → `main` → GitHub Pages. Asset: `?v=20260717`.

---

## Сессия 2026-06 — рефакторинг документации

- Разделение: `agent-plan.md` (горячий) + `docs/agent-spec.md` + `docs/agent-changelog.md`.
- Цель: меньше токенов на старт сессии; сохранить архитектурные решения с обоснованием.

---

## Технический журнал (долгоживущие решения)

### Словарь (2025-06)

- Сборка: iuzhakov → spishniak; `translation-overrides.json` — финальный приоритет.
- Пересборка: `node scripts/build-dictionary.mjs`
- Плохие переводы: править overrides → пересобрать.

### SRS (этап 5)

- Формат: `{ level, checks, due }`; миграция из `{ box, due, history }`.
- Очередь строится один раз при «Начать»; `sessionHistory` — одна запись на шаг.

### HARD RESET

- `hardResetState()` сбрасывает words, phrases, shows, books, knowledge, sessionHistory, стоп-лист.
- Не трогает `data/*.json(.gz)`. Интервалы [1,3,7,16,30] остаются.
- UI: два модала (`#hard-reset-modal` → `#hard-reset-final-modal`); кнопки `modal-actions-stack`; отмена — `btn-dark`.

---

## Git — ключевые коммиты

| Hash | Описание |
|------|----------|
| `8107f1f` | UI: knowledge hub, modal add, trans-chips fix, manual sources, shows/books |
| `8b312ed` | Stage 9 UI: card layout |
| `f118ba5` | Stage 9 UX: knowledge hub, import, stop-list, training |
| `3822a0e` | mobile nav, scroll-top, study lists |
| `7a98562` | prep fix, stop-list translations, desktop shell |
| `f961cd2` | netlify.toml |
| `c9f4989` | GitHub Pages: .nojekyll + deploy-pages.yml |
